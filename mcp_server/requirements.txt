# DSA MCP 服务器依赖
#
# mcp：MCP Python SDK。
#   1.x 用 `mcp.server.fastmcp.FastMCP`，2.x 改名为 `mcp.server.mcpserver.MCPServer`。
#   server.py 做了兼容导入，两个大版本都能跑，这里不锁死上限。
# httpx：HTTP 客户端（DSA 的 REST 调用）。
mcp>=1.2
httpx>=0.27
